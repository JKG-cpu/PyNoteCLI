Whenever a note is created, it should default to a page called All Notes.

In markdown, each note should just be separated by --- (\n in before and after). 

For text, it should just display the text (each note separated by an indent).

Use `from rich.markdown import Markdown` and `from rich.console import Console` to display the markdown text. Then just print() for the normal text.
