### 2026-07-02
Added command's for
-  `pynote config`
-  `pynote page`

Need to create a basic SQL database first in a temp folder, then *(when publishing)*, add the `platformdirs` module.

Gonna start working on the page management after. Need to 
-  Allow page creations
-  Allow different types of pages (normal / checklist, text / markdown)
-  Allow page deletions
-  List pages
-  Display pages
-  Edit pages => ***Need to create a CLI text editor (think of vim)***
